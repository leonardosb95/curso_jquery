--------------AULA01- Introdução----------------------------------------
	jquery da essa compatibilidade de funcionar em todos os navegadores
		escreve uma vez e seu código funciona em todos os navegadores
	escreva menos e faça mais

versão slim
	não possui ajax e nem animações
versão comprimida
	quando o arquivo js vem sem espaço deixando o tamanho menor

jquery tem que ser importado antes do arquivo main
	para eu poder usar no main

$ é o atalho de jQuery()
.text()
	retorna o valor do texto dentro da tag
	podemos colocar valores dentro dele
		.text(numPalavras)

versão 1.12 do jQuery
	recebe atualização até hoje
	versão que da suporte para navegadores antigos
	Internet Explorer 6-8, Opera 12.1x ou Safari 5.1+.

--------------AULA02- Escutando eventos com jQuery----------------------------------------
	campo.on()
		ação que queremos o evento
		campo.on("click")
			ao clicar dispara uma função
	val()
		da acesso a valores que estão dentro de inputs

evento input
	input
		enquanto o usuario digita já atualiza o campo

##Separando com Regexp
	/\S+/
		busca qualquer caracter exceto espaço

--------------AULA03- Game Over com eventos----------------------------------------
evento focus
	o campo ganha foco
	ao entrar no campo

setInterval()
	executa uma função em tempos e tempos
	milesegundos
	para cancelar a função
		clearInterval
			pega o id e cancela o setInterval

adicionar um atributo
	uma função que auxilia
	attr
		campo.attr("rows", 500);//Podemos pegar o valor do atributo e alterar ele no segundo parametro

Escutando um evento uma única vez
	a função on fica escutando o evento toda vez
	a função one()
		escuta uma unica vez somente

--------------AULA04-Reiniciando o jogo----------------------------------------
eventos mais comuns
	click
		quando clica no campo
	blur
		quando sai do campo
	dlblick
		quando clica duas vezes no campo

Organizando o nosso código em funções
	$(document).ready():
		Aguarda a pagina ser carregada e depois executa seu conteudo

--------------AULA05-Funções que auxiliam os estilos----------------------------------------
materialize
	é um framework frontend

CSS com jQuery
	utilizando a função css()
		campo.css("background-color", "lightgray"); //novo


Estilizando o jogo
	estilizar o dom com js


Adicionando e removendo classes facilmente de um elemento
	 campo.addClass("campo-desativado"); //Conseguimos chamar a folha de estilo estilos.css
	 campo.removeClass("campo-desativado");//Conseguimos remover a folha de estilo estilos.css

